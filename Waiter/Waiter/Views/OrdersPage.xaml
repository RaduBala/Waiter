<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Waiter.Views.OrdersPage">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="OdersViewList" ItemsSource="{Binding OrdersData}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Spacing="20" Orientation="Horizontal">
                                    <StackLayout Spacing="10" Orientation="Vertical">
                                        <Label Text="{Binding Order.Title}" FontAttributes="Bold" />
                                        <Label Text="{Binding Order.Price}" VerticalOptions="End" />
                                    </StackLayout>

                                    <StackLayout Spacing="10" Orientation="Horizontal">
                                        <Button Text="-" Clicked="Button_Remove"/>
                                        <Label Text ="{Binding Count}"/>
                                        <Button Text="+" Clicked="Button_Add"/>
                                    </StackLayout>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Text="Commit" Clicked="Button_Clicked" HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>